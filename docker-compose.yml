services:
    server:
        build: ./apps/server
        environment:
            DATABASE_URL: postgres://myuser:mypassword@postgres:5432/mydb
            PORT: 3000
            MIGRATE_ON_START: 'true'
        ports: ['3000:3000']
        depends_on: [postgres]

volumes:
    pgdata:
